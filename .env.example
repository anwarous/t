# URL of the backend API (Spring Boot).
# Leave empty (or omit the file entirely) to use built-in demo mode,
# which stores users in localStorage — no server required.
VITE_API_BASE_URL=http://localhost:8080/api
